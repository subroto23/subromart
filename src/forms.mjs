import { products, services, bi } from './data/content.mjs';
import { profile, tr, href, icon, waIcon, button } from './ui.mjs';
export function contactForm(lang,mode='contact'){
 const t=v=>tr(v,lang),demo=mode==='demo',planner=mode==='planner';
 const field=(name,label,type='text',required=true,placeholder='',extra='')=>`<label class="field">${label}${required?' <span aria-hidden="true">*</span>':''}<input name="${name}" type="${type}" ${required?'required':''} ${placeholder?`placeholder="${placeholder}"`:''} ${extra}></label>`;
 return `<form class="brief-form" id="brief-form" data-form-mode="${mode}"><div class="form-heading"><span class="form-icon">${icon(demo?'monitor':planner?'pen':'message')}</span><div><h2>${t(demo?bi('Let’s plan your demo','আপনার ডেমো পরিকল্পনা করি'):planner?bi('Your project, in a few steps','কয়েক ধাপে আপনার প্রজেক্ট'):bi('Your project brief','আপনার প্রজেক্ট ব্রিফ'))}</h2></div></div>${planner?`<ol class="form-steps js-only"><li data-step-indicator="0" class="active"><span>1</span>${t(bi('Your idea','আপনার আইডিয়া'))}</li><li data-step-indicator="1"><span>2</span>${t(bi('The details','বিস্তারিত'))}</li><li data-step-indicator="2"><span>3</span>${t(bi('Let’s connect','যোগাযোগ'))}</li></ol>`:''}
 <fieldset class="form-section" ${planner?'data-form-step="0"':''}><legend>${t(bi('What would you like to build?','কী তৈরি করতে চান?'))}</legend><label class="field">${t(bi('Interested in','আগ্রহের বিষয়'))} <span aria-hidden="true">*</span><select name="interest" required><option value="">${t(bi('Select a solution or service','সলিউশন বা সেবা বাছাই করুন'))}</option><optgroup label="${t(bi('Software solutions','সফটওয়্যার সলিউশন'))}">${products.map(p=>`<option value="${p.id}">${p.name} — ${t(p.label)}</option>`).join('')}</optgroup><optgroup label="${t(bi('Services','সেবাসমূহ'))}">${services.map(s=>`<option value="${s.id}">${t(s.title)}</option>`).join('')}</optgroup><option value="not-sure">${t(bi('I’d like your advice','আপনাদের পরামর্শ চাই'))}</option></select></label><label class="field">${t(bi('Your project or challenge','আপনার প্রজেক্ট বা সমস্যা'))} <span aria-hidden="true">*</span><textarea name="message" rows="${planner?5:4}" minlength="10" maxlength="3000" required placeholder="${t(bi('What should the software do? Who will use it?','কী করতে চান? কারা ব্যবহার করবেন? কোন কাজ সহজ করতে চান?'))}"></textarea><small>${t(bi('At least 10 characters. Please don’t include passwords or sensitive records.','কমপক্ষে ১০ অক্ষর। পাসওয়ার্ড বা সংবেদনশীল রেকর্ড লিখবেন না।'))}</small></label>${planner?`<label class="field">${t(bi('Your industry','আপনার খাত'))}<select name="industry"><option value="">${t(bi('Choose your industry','আপনার খাত বাছাই করুন'))}</option>${[bi('Education','শিক্ষা'),bi('Healthcare','স্বাস্থ্যসেবা'),bi('Retail & commerce','রিটেইল ও কমার্স'),bi('Professional services','পেশাগত সেবা'),bi('Other industry','অন্যান্য')].map(v=>`<option>${t(v)}</option>`).join('')}</select></label>`:''}</fieldset>
 ${planner?`<fieldset class="form-section" data-form-step="1"><legend>${t(bi('A little more detail','আরেকটু বিস্তারিত'))}</legend><p class="muted">${t(bi('Select what matters. We’ll help refine the scope together.','প্রয়োজনীয় বিষয় বেছে নিন। কাজের পরিধি একসাথে ঠিক করব।'))}</p><div class="feature-options">${[bi('Admin dashboard','অ্যাডমিন ড্যাশবোর্ড'),bi('User accounts','ইউজার অ্যাকাউন্ট'),bi('Online payments','অনলাইন পেমেন্ট'),bi('Mobile app','মোবাইল অ্যাপ'),bi('Reports & analytics','রিপোর্ট ও অ্যানালিটিক্স'),bi('Notifications','নোটিফিকেশন'),bi('Bengali & English','বাংলা ও ইংরেজি'),bi('Ongoing maintenance','নিয়মিত মেইনটেন্যান্স')].map(v=>`<label><input type="checkbox" name="features" value="${t(v)}"><span>${t(v)}</span></label>`).join('')}</div><div class="form-grid"><label class="field">${t(bi('Budget range','বাজেটের ধারণা'))}<select name="budget"><option value="To be discussed">${t(bi('Let’s discuss','আলোচনা করে ঠিক করব'))}</option><option>৳50,000 – ৳150,000</option><option>৳150,000 – ৳500,000</option><option>৳500,000+</option></select><small>${t(bi('Your budget preference, not a price quote.','আপনার বাজেটের পছন্দ, মূল্য প্রস্তাব নয়।'))}</small></label><label class="field">${t(bi('Preferred timeline','পছন্দের সময়সীমা'))}<select name="timeline"><option>${t(bi('Flexible','আলোচনা সাপেক্ষে'))}</option><option>${t(bi('Within 1 month','১ মাসের মধ্যে'))}</option><option>${t(bi('1–3 months','১–৩ মাস'))}</option><option>${t(bi('3–6 months','৩–৬ মাস'))}</option></select></label></div></fieldset>`:''}
 <fieldset class="form-section" ${planner?'data-form-step="2"':''}><legend>${t(bi('How can we reach you?','কীভাবে যোগাযোগ করব?'))}</legend><div class="form-grid">${field('name',t(bi('Your name','আপনার নাম')),'text',true,t(bi('Full name','পুরো নাম')),'autocomplete="name" maxlength="100"')}${field('email',t(bi('Email address','ইমেইল')),'email',true,'you@company.com','autocomplete="email" maxlength="160"')}${field('company',t(bi('Company or organisation','প্রতিষ্ঠানের নাম')),'text',false,t(bi('Your company','আপনার প্রতিষ্ঠান')),'autocomplete="organization" maxlength="140"')}${field('phone',t(bi('Phone / WhatsApp','ফোন / WhatsApp')),'tel',false,'+880…','autocomplete="tel" maxlength="30"')}</div>${demo?`<div class="form-grid">${field('preferredDate',t(bi('Preferred date','পছন্দের তারিখ')),'date',false)}<label class="field">${t(bi('Preferred time (Dhaka, UTC+6)','পছন্দের সময় (ঢাকা, UTC+6)'))}<select name="preferredTime"><option>${t(bi('Flexible','আলোচনা সাপেক্ষে'))}</option><option>10:00 – 12:00</option><option>14:00 – 16:00</option><option>18:00 – 20:00</option></select></label></div><p class="form-note">${icon('clock')}${t(bi('This is a request, not a confirmed booking. We’ll agree the time with you.','এটি একটি অনুরোধ, নিশ্চিত বুকিং নয়। আপনার সাথে সময় ঠিক করে নেওয়া হবে।'))}</p>`:''}<label class="consent-row"><input type="checkbox" name="consent" required><span>${t(bi('I agree to share these details to discuss my project.','প্রজেক্ট নিয়ে আলোচনার জন্য এই তথ্য শেয়ার করতে সম্মত।'))} <a href="${href('privacy',lang)}">${t(bi('Privacy information','গোপনীয়তার তথ্য'))}</a></span></label></fieldset>
 <div class="form-actions">${planner?`<button type="button" class="btn btn-secondary js-only" id="form-back" hidden>← ${t(bi('Back','পিছনে'))}</button><button type="button" class="btn btn-dark js-only" id="form-next">${t(bi('Continue','পরের ধাপ'))}${icon('arrow')}</button>`:''}<button type="submit" class="btn btn-primary js-only" id="prepare-brief">${t(bi('Prepare my message','আমার বার্তা প্রস্তুত করুন'))}${icon('arrow')}</button></div><p class="form-privacy">${icon('lock')}${t(bi('Nothing is sent automatically. Review your message, then choose WhatsApp or email.','স্বয়ংক্রিয়ভাবে কিছু পাঠানো হবে না। বার্তা দেখে WhatsApp বা ইমেইল বেছে নিন।'))}</p><noscript><p>${t(bi('Please use the email or WhatsApp links on this page to share your project details.','এই পেজের ইমেইল বা WhatsApp লিংক দিয়ে প্রজেক্টের তথ্য জানান।'))}</p></noscript></form>
 <section class="brief-result" id="brief-result" aria-labelledby="result-title" hidden tabindex="-1"><span class="result-icon">${icon('check')}</span><p class="eyebrow">${t(bi('READY WHEN YOU ARE','আপনি প্রস্তুত হলেই'))}</p><h2 id="result-title">${t(bi('Your message is ready.','আপনার বার্তা প্রস্তুত।'))}</h2><p>${t(bi('Review it below. Choose a channel to open your message and send it yourself. It has not been sent yet.','নিচে দেখে নিন। একটি মাধ্যম বেছে নিয়ে বার্তাটি নিজে পাঠান। এখনও পাঠানো হয়নি।'))}</p><pre id="brief-preview"></pre><div class="button-row"><a id="brief-whatsapp" class="btn btn-primary" target="_blank" rel="noopener noreferrer">${waIcon()}${t(bi('Open WhatsApp','WhatsApp খুলুন'))}</a><a id="brief-email" class="btn btn-secondary">${icon('mail')}${t(bi('Open email','ইমেইল খুলুন'))}</a></div><div class="result-tools"><button type="button" class="text-link" id="download-brief">${icon('download')}${t(bi('Download brief','ব্রিফ ডাউনলোড'))}</button><button type="button" class="text-link" id="copy-brief">${icon('copy')}${t(bi('Copy message','বার্তা কপি'))}</button><button type="button" class="text-link" id="edit-brief">${icon('pen')}${t(bi('Edit details','তথ্য পরিবর্তন'))}</button></div></section>`;
}
export function contactAside(lang){const t=v=>tr(v,lang);return `<aside class="contact-aside"><p class="eyebrow"><span></span>${t(bi('SAY HELLO, DIRECTLY','সরাসরি কথা বলুন'))}</p><h2>${t(bi('Direct contact.','সরাসরি যোগাযোগ।'))}</h2><div class="contact-person"><img src="/assets/subroto-avatar.webp" alt="${profile.name}" width="64" height="64"><div><strong>${t(bi('Subroto Das','সুব্রত দাস'))}</strong><span>${t(bi('Founder & CEO','প্রতিষ্ঠাতা ও CEO'))}</span></div></div><div class="contact-methods"><a href="${profile.whatsapp}" target="_blank" rel="noopener noreferrer">${waIcon()}<span><small>WhatsApp</small><strong>${profile.phones[0]}</strong></span>${icon('up')}</a><a href="mailto:${profile.email}">${icon('mail')}<span><small>${t(bi('Email','ইমেইল'))}</small><strong>${profile.email}</strong></span>${icon('up')}</a><a href="tel:${profile.phones[1].replaceAll(' ','')}">${icon('call')}<span><small>${t(bi('Call us','ফোন করুন'))}</small><strong>${profile.phones[1]}</strong></span>${icon('up')}</a><div>${icon('pin')}<span><small>${t(bi('Based in','আমাদের অবস্থান'))}</small><strong>${t(bi(profile.location,profile.locationBn))}</strong></span></div></div><a class="source-link" href="${profile.website}/contact" target="_blank" rel="noopener noreferrer">${t(bi('Contact details from the founder’s profile','প্রতিষ্ঠাতার প্রোফাইল থেকে যোগাযোগের তথ্য'))}${icon('up')}</a></aside>`;}
