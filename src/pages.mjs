import { productSummaries, serviceSummaries, pageIntros } from './data/short-copy.mjs';
import { productDetails } from './data/product-details.mjs';
import { home } from './home.mjs';
import { products, services, projects, guides, processSteps, faqs, bi } from './data/content.mjs';
import { profile, esc, tr, href, icon, waIcon, button, sectionHead, ticks, devices, dashboard, productCard, serviceCard, processBlock, faqBlock, ctaBlock } from './ui.mjs';
import { contactForm, contactAside } from './forms.mjs';
import { aboutPage, policyPage } from './editorial.mjs';
import { founderPage, clientCarePage } from './trust.mjs';

export function renderPage(page,lang){
 const t=v=>tr(v,lang);
 switch(page.kind){
 case 'home':return home(lang);
 case 'product':return productPage(page,lang);
 case 'service':return servicePage(page,lang);
 case 'solutions':return `${intro(page,lang,t(bi('FIND YOUR STARTING POINT','আপনার শুরুটা খুঁজে নিন')))}<section class="container catalogue-section"><div class="catalogue-toolbar">${filters(lang)}<label class="catalogue-search js-only">${icon('search')}<input type="search" id="catalogue-search" aria-label="${t(bi('Search solutions','সলিউশন খুঁজুন'))}" placeholder="${t(bi('Find a solution…','সলিউশন খুঁজুন…'))}"></label></div><p class="filter-count js-only" id="filter-count" aria-live="polite"></p><div class="product-grid filter-grid">${products.map(p=>productCard(p,lang)).join('')}</div><div class="empty-state" id="filter-empty" hidden>${icon('search')}<h2>${t(bi('Let’s try another search.','অন্যভাবে খুঁজে দেখুন।'))}</h2><p>${t(bi('No matching solutions. Try a different word or clear your filters.','মিলে যায় এমন সলিউশন পাওয়া যায়নি। অন্য শব্দ ব্যবহার করুন বা ফিল্টার মুছুন।'))}</p><button type="button" class="btn btn-secondary" data-reset-filters>${t(bi('Clear filters','ফিল্টার মুছুন'))}</button></div><p class="section-note">${icon('spark')}${t(bi('Solution concepts are configured to your scope. Product previews use illustrative data.','সলিউশন আপনার কাজের পরিধি অনুযায়ী কনফিগার করা হবে। প্রিভিউয়ের তথ্য নমুনা।'))}</p></section>${ctaBlock(lang)}`;
 case 'services':return `${intro(page,lang,t(bi('YOUR EXTENDED TECH TEAM','আপনার প্রযুক্তি টিম')),button(t(bi('Plan your project','প্রজেক্ট পরিকল্পনা করুন')),href('project-planner',lang)))}<section class="container section"><div class="service-grid three">${services.map((s,i)=>serviceCard(s,lang,i)).join('')}</div></section><section class="section process-section"><div class="container">${sectionHead(t(bi('BUILT TOGETHER','একসাথে তৈরি')),t(bi('From first conversation to what’s next.','প্রথম আলোচনা থেকে পরবর্তী পথচলা।')))}${processBlock(lang)}</div></section>${ctaBlock(lang)}`;
 case 'work':return `${intro(page,lang,t(bi('CRAFT, IN PRACTICE','অভিজ্ঞতার বাস্তব প্রয়োগ')))}<section class="container catalogue-section"><div class="attribution-note">${icon('briefcase')}<p>${t(bi('The founder’s professional work and learning resources; not Subromart client commissions or resale products. Visuals use sample data.','প্রতিষ্ঠাতার পেশাগত কাজ ও শেখার রিসোর্স; Subromart-এর ক্লায়েন্ট প্রজেক্ট বা বিক্রির পণ্য নয়। ভিজ্যুয়ালগুলো নমুনা।'))}</p></div>${filters(lang,true)}<div class="product-grid filter-grid">${projects.map(p=>`<article class="product-card" data-category="${p.category}"><div class="product-visual visual-${p.demo}"><div class="visual-top"><span class="tiny-pill">${icon(p.icon)}${t(p.type)}</span></div><div class="product-window"><img src="/assets/previews/${p.demo}-${lang}.webp" width="720" height="490" alt="" loading="lazy" decoding="async"></div></div><div class="product-info"><h3>${p.name}</h3><p>${t(p.description)}</p><div class="product-tags">${p.tags.map(x=>`<span>${x}</span>`).join('')}</div><a class="text-link" href="${p.url}" target="_blank" rel="noopener noreferrer">${t(bi('View original work','মূল কাজ দেখুন'))}${icon('up')}</a></div></article>`).join('')}</div></section>${ctaBlock(lang)}`;
 case 'how-we-work':return `${intro(page,lang,t(bi('NO MYSTERY. SHARED MOMENTUM.','পরিষ্কার কাজ। একসাথে অগ্রগতি।')))}<section class="section container"><div class="process-detail-list">${processSteps.map((s,i)=>`<article><span class="large-step">0${i+1}</span><div><p class="eyebrow">${t(s.deliverable)}</p><h2>${t(s.title)}</h2>${ticks(([[bi('Identify users, priorities and constraints','ব্যবহারকারী, অগ্রাধিকার ও সীমাবদ্ধতা নির্ধারণ'),bi('Document the first useful release','কার্যকর প্রথম সংস্করণ লিখে রাখা'),bi('Agree scope, cost and responsibilities','পরিধি, খরচ ও দায়িত্ব নির্ধারণ')],[bi('Review wireframes and key screens','ওয়্যারফ্রেম ও জরুরি স্ক্রিন দেখা'),bi('Map data and integration requirements','তথ্য ও ইন্টিগ্রেশনের চাহিদা নির্ধারণ'),bi('Approve a practical delivery roadmap','বাস্তবসম্মত ডেলিভারি পরিকল্পনা অনুমোদন')],[bi('Build in reviewable milestones','পর্যালোচনাযোগ্য মাইলস্টোনে কাজ'),bi('Test core journeys and failure states','মূল কাজ ও ত্রুটির অবস্থা পরীক্ষা'),bi('Document feedback and change requests','মতামত ও পরিবর্তনের অনুরোধ লিখে রাখা')],[bi('Prepare access, accounts and deployment','অ্যাকসেস, অ্যাকাউন্ট ও ডেপ্লয়মেন্ট প্রস্তুত'),bi('Train your team and hand over documentation','টিমকে প্রশিক্ষণ ও ডকুমেন্টেশন দেওয়া'),bi('Agree ongoing support and next improvements','নিয়মিত সাপোর্ট ও পরবর্তী উন্নয়ন ঠিক করা')]])[i],lang)}</div></article>`).join('')}</div></section>${ctaBlock(lang)}`;
 case 'pricing':return pricing(lang,page);
 case 'about':return aboutPage(lang);
 case 'subroto-das':return founderPage(lang);
 case 'client-care':return clientCarePage(lang);
 case 'contact':case 'book-demo':case 'project-planner':return `${intro(page,lang,t(bi('CONTACT SUBROMART','SUBROMART-এর সঙ্গে যোগাযোগ')))}<section class="container contact-layout">${contactAside(lang)}<div class="form-container">${contactForm(lang,page.kind==='book-demo'?'demo':page.kind==='project-planner'?'planner':'contact')}</div></section>`;
 case 'faq':return `${intro(page,lang,t(bi('THE DETAILS THAT MATTER','যা জানা প্রয়োজন')))}<section class="container narrow section">${faqBlock(lang,faqs.length)}</section>${ctaBlock(lang)}`;
 case 'resources':return `${intro(page,lang,t(bi('A LITTLE KNOWLEDGE GOES A LONG WAY','জানা থাকলে সিদ্ধান্ত সহজ হয়')))}<section class="container section"><div class="resource-grid">${guides.map((g,i)=>`<article class="resource-card"><a href="${href('resources/'+g.id,lang)}" class="resource-art art-${i}">${icon(g.icon)}<span>FIELD NOTES / 0${i+1}</span></a><p class="eyebrow">${t(g.category)}</p><h2><a href="${href('resources/'+g.id,lang)}">${t(g.title)}</a></h2><p>${t(g.description)}</p><a class="text-link" href="${href('resources/'+g.id,lang)}">${t(bi('Read the guide','গাইডটি পড়ুন'))}${icon('arrow')}</a></article>`).join('')}</div><div class="support-banner"><div><h2>${t(bi('Curious about what’s under the hood?','ভেতরের প্রযুক্তি জানতে আগ্রহী?'))}</h2><p>${t(bi('Explore visual engineering walkthroughs on Systems Hub, the founder’s personal learning platform.','প্রতিষ্ঠাতার ব্যক্তিগত শেখার প্ল্যাটফর্ম Systems Hub-এ ভিজ্যুয়াল ইঞ্জিনিয়ারিং ব্যাখ্যা দেখুন।'))}</p></div>${button(t(bi('Explore Systems Hub','Systems Hub দেখুন')),profile.systems,'dark','up','target="_blank" rel="noopener noreferrer"')}</div></section>${ctaBlock(lang)}`;
 case 'guide':return `${intro(page,lang,t(page.item.category))}<div class="container article-meta"><span>${icon('pen')}${t(bi('Subromart editorial','Subromart সম্পাদকীয়'))}</span><a href="${href('subroto-das',lang)}">${t(bi('Meet our founder','প্রতিষ্ঠাতাকে জানুন'))}</a><span>${icon('clock')}${Math.max(1,Math.ceil(page.item.sections.map(([,text])=>t(text)).join(' ').split(/\s+/).length/180))} ${t(bi('min read','মিনিটে পড়ুন'))}</span></div><div class="container prose-layout"><aside class="prose-toc"><strong>${t(bi('In this guide','এই গাইডে'))}</strong>${page.item.sections.map(([h],i)=>`<a href="#guide-${i}">${t(h)}</a>`).join('')}</aside><article class="prose">${page.item.sections.map(([h,p],i)=>`<section id="guide-${i}"><h2>${t(h)}</h2><p>${t(p)}</p></section>`).join('')}${page.item.related?`<nav class="guide-related" aria-label="${t(bi('Related resources','সংশ্লিষ্ট রিসোর্স'))}">${page.item.related.map(([slug,label])=>`<a class="text-link" href="${href(slug,lang)}">${t(label)}${icon('arrow')}</a>`).join('')}</nav>`:''}<div class="article-next"><h3>${t(bi('Put your idea into words.','আপনার ভাবনাটি লিখে ফেলুন।'))}</h3>${button(t(bi('Create a project brief','প্রজেক্ট ব্রিফ তৈরি করুন')),href('project-planner',lang))}</div></article></div>${ctaBlock(lang)}`;
 case 'compare':return `${intro(page,lang,t(bi('A CLEARER CHOICE','বেছে নেওয়া হোক সহজ')))}<section class="container section"><div id="compare-content"></div><noscript><p>${t(bi('Enable JavaScript to compare selections, or explore the individual solution pages.','তুলনার জন্য JavaScript চালু করুন বা আলাদা সলিউশন পেজ দেখুন।'))}</p></noscript><div class="compare-picker"><h2>${t(bi('Add a solution to compare','তুলনার জন্য সলিউশন যোগ করুন'))}</h2><p>${t(bi('Choose up to three. Your selection stays on this device.','সর্বোচ্চ তিনটি বেছে নিন। এই ডিভাইসে বাছাই মনে থাকবে।'))}</p><div class="compare-options">${products.map(p=>`<button type="button" class="compare-option js-only" data-compare="${p.id}" aria-pressed="false">${icon(p.icon)}<span><b>${p.name}</b><small>${t(p.label)}</small></span>${icon('plus')}</button>`).join('')}</div></div></section>${ctaBlock(lang)}`;
 case 'search':return `${intro(page,lang,t(bi('EVERYTHING, A LITTLE CLOSER','সব তথ্য হাতের নাগালে')))}<section class="container narrow section"><form class="search-form" action="${href('search',lang)}"><label class="sr-only" for="page-search">${t(bi('Search website','ওয়েবসাইটে খুঁজুন'))}</label><div class="search-input-wrap">${icon('search')}<input type="search" name="q" id="page-search" placeholder="${t(bi('Search solutions, services and guides…','সলিউশন, সেবা ও গাইড খুঁজুন…'))}" autocomplete="off"><button type="submit" class="icon-btn" aria-label="${t(bi('Search','খুঁজুন'))}">${icon('arrow')}</button></div></form><div id="page-results" class="search-results" aria-live="polite"></div><noscript><p>${t(bi('For search, enable JavaScript. You can also browse all solutions and services using the navigation.','সার্চের জন্য JavaScript চালু করুন। নেভিগেশন থেকে সব সলিউশন ও সেবা দেখতে পারবেন।'))}</p></noscript></section>`;
 case 'privacy':case 'terms':case 'accessibility':return `${intro(page,lang,t(bi('CLEAR BY DESIGN','পরিষ্কারভাবে জানুন')))}${policyPage(page.kind,lang)}`;
 case '404':return `<section class="container error-page"><span class="error-code">404</span><h1>${t(bi('This page took a different turn.','এই পেজটি পাওয়া যাচ্ছে না।'))}</h1><p>${t(bi('The link may have changed. Let’s get you back to something useful.','লিংকটি বদলে যেতে পারে। প্রয়োজনীয় পেজে ফিরে যাই।'))}</p><div class="button-row">${button(t(bi('Back to home','হোমে ফিরুন')),href('',lang))}${button(t(bi('Explore solutions','সলিউশন দেখুন')),href('solutions',lang),'secondary')}</div></section>`;
 default:throw new Error('Missing page renderer: '+page.kind);
 }
}

export function intro(page,lang,eyebrow,actions=''){const t=v=>tr(v,lang),copy=pageIntros[page.kind];return `<section class="page-intro container"><p class="eyebrow"><span></span>${eyebrow||t(bi('THE SUBROMART WAY','SUBROMART-এর সাথে'))}</p><h1>${t(copy?.[0] || page.title)}</h1><p>${t(page.kind==='service'?serviceSummaries[page.item.id]:(copy?.[1] || page.description))}</p>${actions?`<div class="button-row">${actions}</div>`:''}</section>`;}
const filters=(lang,project=false)=>{const t=v=>tr(v,lang);return `<div class="filter-row js-only" aria-label="${t(bi('Filter by category','ক্যাটাগরি বাছাই'))}">${(project?[['all',bi('All work','সব কাজ')],['web',bi('Web platforms','ওয়েব প্ল্যাটফর্ম')],['mobile',bi('Mobile apps','মোবাইল অ্যাপ')],['backend',bi('Backend & systems','ব্যাকএন্ড ও সিস্টেম')]]:[['all',bi('All solutions','সব সলিউশন')],['education',bi('Education','শিক্ষা')],['healthcare',bi('Healthcare','স্বাস্থ্যসেবা')],['commerce',bi('Commerce','কমার্স')],['business',bi('Business','ব্যবসা')]]).map(([v,label],i)=>`<button type="button" class="filter-chip ${i===0?'active':''}" data-filter="${v}" aria-pressed="${i===0}">${t(label)}</button>`).join('')}</div>`;};


function relatedLinks(items,base,lang){const t=v=>tr(v,lang);return `<nav class="related-links" aria-label="${t(bi('Related options','সংশ্লিষ্ট সেবা'))}">${items.map(item=>`<a href="${href(base+'/'+item.id,lang)}">${t(item.label||item.title)}${icon('arrow')}</a>`).join('')}</nav>`;}

function productPage(page,lang){
 const t=v=>tr(v,lang),p=page.item;
 return `<section class="product-hero container"><div><p class="eyebrow">${p.name}</p><h1>${t(p.label)}</h1><p class="lead">${t(productSummaries[p.id])}</p><div class="button-row">${button(t(bi('Request a demo','ডেমো চাই')),href('book-demo',lang)+'?interest='+p.id)}<button type="button" class="btn btn-secondary js-only" data-demo="${p.demo}">${icon('play')}${t(bi('Explore preview','প্রিভিউ দেখুন'))}</button></div><p class="product-audience">${icon('users')}<span>${t(p.audience)}</span></p></div><div class="product-hero-preview"><div class="browser-bar"><span><i></i><i></i><i></i></span><span>${p.name.toLowerCase()}.workspace</span>${icon('lock')}</div>${dashboard(p.demo,lang)}<p class="preview-caption">${t(bi('Concept preview · Sample data','কনসেপ্ট প্রিভিউ · নমুনা তথ্য'))}</p></div></section>
 <section class="section container product-modules">${sectionHead(t(bi('YOUR TOOLKIT','প্রয়োজনীয় মডিউল')),t(bi('The features that matter.','কাজের ফিচারগুলো।')),t(bi('Open a module to explore. Final scope is agreed with you.','বিস্তারিত জানতে মডিউল খুলুন। চূড়ান্ত ফিচার একসঙ্গে ঠিক করব।')))}<div class="module-list">${p.features.map((f,i)=>`<details class="module-detail"><summary><span class="module-number">0${i+1}</span><h3>${t(f)}</h3>${icon('plus')}</summary><p>${t(productDetails[p.id][i])}</p></details>`).join('')}</div>
 <details class="delivery-details"><summary>${icon('settings')}<span>${t(bi('Setup, integrations & ongoing support','সেটআপ, ইন্টিগ্রেশন ও নিয়মিত সাপোর্ট'))}</span>${icon('plus')}</summary><div><ol>${p.workflow.map(step=>`<li>${t(step)}</li>`).join('')}</ol><p>${t(p.integrations)}</p><p>${t(bi('Training, handover, licensing and maintenance are defined in your proposal.','প্রশিক্ষণ, হস্তান্তর, লাইসেন্স ও মেইনটেন্যান্স প্রস্তাবে নির্ধারিত হবে।'))}</p><a class="text-link" href="${href('services/maintenance',lang)}">${t(bi('Maintenance options','মেইনটেন্যান্স সেবা'))}${icon('arrow')}</a></div></details></section>
 <section class="section container compact-related"><h2>${t(bi('Also explore','আরও দেখুন'))}</h2>${relatedLinks(products.filter(x=>x.id!==p.id).slice(0,3),'solutions',lang)}<div class="guide-related"><a class="text-link" href="${href('client-care',lang)}">${t(bi('Scope, ownership & support checklist','কাজ, মালিকানা ও সাপোর্টের চেকলিস্ট'))}${icon('arrow')}</a>${p.id==='ecommerce'?`<a class="text-link" href="${href('resources/ecommerce-website-checklist',lang)}">${t(bi('Plan your online store','অনলাইন স্টোরের পরিকল্পনা'))}${icon('arrow')}</a>`:''}</div></section>${ctaBlock(lang)}`;
}

function servicePage(page,lang){
 const t=v=>tr(v,lang),s=page.item;
 return `${intro(page,lang,t(bi('OUR SERVICES','আমাদের সেবা')),button(t(bi('Discuss your project','প্রজেক্ট নিয়ে কথা বলুন')),href('contact',lang)+'?interest='+s.id))}<section class="section container service-detail"><div class="service-statement"><span class="statement-icon">${icon(s.icon)}</span><h2>${t(s.short)}</h2><details class="delivery-details"><summary><span>${t(bi('How this service works','সেবাটি যেভাবে কাজ করে'))}</span>${icon('plus')}</summary><div><p>${t(s.detail)}</p></div></details><a class="text-link" href="${href('how-we-work',lang)}">${t(bi('Our delivery process','কাজের ধাপগুলো'))}${icon('arrow')}</a></div><div class="scope-card"><p class="eyebrow">${t(bi('WHAT WE COVER','যে কাজগুলো করি'))}</p>${ticks(s.features,lang)}<a class="text-link" href="${href('project-planner',lang)}?interest=${s.id}">${t(bi('Plan your scope','কাজের পরিধি ঠিক করুন'))}${icon('arrow')}</a></div></section><section class="section container compact-related"><h2>${t(bi('Related services','সংশ্লিষ্ট সেবা'))}</h2>${relatedLinks(services.filter(x=>x.id!==s.id).slice(0,3),'services',lang)}</section>${ctaBlock(lang)}`;
}

function pricing(lang,page){const t=v=>tr(v,lang);const plans=[{title:bi('Configure','কনফিগার করুন'),label:bi('CONFIGURED SOLUTION','কনফিগার করা সলিউশন'),icon:'grid',text:bi('A focused software solution, configured to your business.','আপনার ব্যবসার প্রয়োজন অনুযায়ী সাজানো সফটওয়্যার সলিউশন।'),items:[bi('Requirement & module selection','চাহিদা ও মডিউল নির্বাচন'),bi('Branding & configuration','ব্র্যান্ডিং ও কনফিগারেশন'),bi('Onboarding & training plan','অনবোর্ডিং ও প্রশিক্ষণ পরিকল্পনা'),bi('Optional maintenance plan','প্রয়োজনে মেইনটেন্যান্স প্ল্যান')],url:'solutions'},{title:bi('Build custom','কাস্টম তৈরি করুন'),label:bi('CUSTOM DEVELOPMENT','কাস্টম ডেভেলপমেন্ট'),icon:'code',text:bi('A product built around your unique workflows and goals.','আপনার নিজস্ব কাজের ধরন ও লক্ষ্য অনুযায়ী তৈরি প্রোডাক্ট।'),items:[bi('Discovery & product design','চাহিদা বিশ্লেষণ ও ডিজাইন'),bi('Frontend, backend & integrations','ফ্রন্টএন্ড, ব্যাকএন্ড ও ইন্টিগ্রেশন'),bi('Testing & phased delivery','পরীক্ষা ও ধাপে ধাপে ডেলিভারি'),bi('Documentation & handover','ডকুমেন্টেশন ও হস্তান্তর')],url:'project-planner'},{title:bi('Maintain','মেইনটেইন করুন'),label:bi('MANAGED CARE','ম্যানেজড কেয়ার'),icon:'shield',text:bi('A clear support plan to keep your software moving forward.','সফটওয়্যার এগিয়ে রাখতে পরিষ্কার সাপোর্ট পরিকল্পনা।'),items:[bi('System review & onboarding','সিস্টেম পর্যালোচনা ও অনবোর্ডিং'),bi('Agreed monitoring & backups','নির্ধারিত মনিটরিং ও ব্যাকআপ'),bi('Updates & bug fixes','আপডেট ও বাগ ফিক্স'),bi('Defined support & review cycle','নির্ধারিত সাপোর্ট ও রিভিউ')],url:'contact?interest=maintenance'}];return `${intro(page,lang,t(bi('A PLAN THAT FITS','মানানসই পরিকল্পনা')))}<section class="container pricing-grid">${plans.map((p,i)=>`<article class="pricing-card ${i===1?'featured':''}">${i===1?`<span class="plan-ribbon">${t(bi('BUILT AROUND YOU','আপনাকে ঘিরেই'))}</span>`:''}<span class="plan-icon">${icon(p.icon)}</span><p class="eyebrow">${t(p.label)}</p><h2>${t(p.title)}</h2><p>${t(p.text)}</p><div class="quote-price">${t(bi('Priced to your scope','কাজ অনুযায়ী মূল্য'))}</div>${ticks(p.items,lang)}${button(t(bi('Explore this approach','এই পরিকল্পনা দেখুন')),p.url.includes('?')?href('contact',lang)+'?interest=maintenance':href(p.url,lang),i===1?'primary':'secondary')}</article>`).join('')}</section><section class="section container pricing-details"><details class="delivery-details"><summary><span>${t(bi('Pricing, ownership & third-party costs','মূল্য, মালিকানা ও তৃতীয় পক্ষের খরচ'))}</span>${icon('plus')}</summary><div><p>${t(bi('Your proposal defines deliverables, payment milestones, licensing and ownership. Support and third-party costs—hosting, payment fees, messages and app-store accounts—are listed separately.','প্রস্তাবে কাজ, পেমেন্টের ধাপ, লাইসেন্স ও মালিকানা নির্ধারিত হয়। সাপোর্ট ও তৃতীয় পক্ষের খরচ—হোস্টিং, পেমেন্ট ফি, মেসেজিং ও অ্যাপ স্টোর অ্যাকাউন্ট—আলাদা থাকে।'))}</p><p>${t(bi('Contact us for a quote. This website has no automatic checkout.','মূল্য জানতে যোগাযোগ করুন। এখানে স্বয়ংক্রিয় চেকআউট নেই।'))}</p></div></details></section>${ctaBlock(lang)}`;}
